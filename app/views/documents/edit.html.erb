<div class="row">
  <div class="large-9 large-centered small-12 columns">
    <fieldset>
      <legend><%= t :edit %> <%= @document.name.capitalize %> <%= t :documents %></legend>
      <div class="panel"><%= t :documents_edit_description %></div>
      <%= render 'form' %>
    </fieldset>
  </div>
</div>
