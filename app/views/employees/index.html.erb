<div class="row">
  <div class="large-12 columns">
    <h2>Employee's List</h2>
    <table>
      <thead>
        <th>Last Name</th>
        <th>First Name</th>
        <th>Position</th>
        <th>E-mail</th>
        <th>Mobile Contact</th>
        <th>Address</th>
        <th></th>
        <th></th>
      </thead>
      <% @employees.each do |employee|%>
        <tbody>
          <td><%= employee.last_name%></td>
          <td><%= employee.first_name%></td>
          <td><%= employee.position.name if !employee.position.blank?%></td>
          <td><%= employee.account.email if !employee.account.nil?%></td>
          <td><%= employee.mobile_contact%></td>
          <td><%= employee.address%></td>
          <td><%= link_to "Show", organization_employee_path(@organization, employee)%></td>
          <td><%= link_to "Edit", edit_organization_employee_path(@organization, employee)%></td>
        </tbody>
      <% end %>
    </table>
    <%= link_to "Add New Employee", new_organization_employee_path, class: "button" %>
  </div>
</div>


