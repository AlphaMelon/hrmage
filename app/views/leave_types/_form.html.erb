<div class="row">
  <div class="large-8 small-12 columns">
    <% if !@leave_type.new_record? %>
      <%= simple_form_for [current_organization, @leave_type], wrapper: :custom, url: organization_leave_type_path(current_organization, @leave_type) do |f| %>
        <%= render 'fields', f: f %>
      <% end %>
    <% else %>
      <%= simple_form_for [current_organization, @leave_type], wrapper: :custom do |f| %>
        <%= render 'fields', f: f %>
      <% end %>
    <% end %>
  </div>
</div>
