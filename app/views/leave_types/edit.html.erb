<div class="row">
  <div class="large-9 large-centered small-12 columns">
    <fieldset>
      <legend><%= t :edit %> <%= @leave_type.name %></legend>
      <div class="panel"><%= t :leave_types_edit_description %></div>
      <%= render 'form' %>
    </fieldset>
  </div>
</div>
