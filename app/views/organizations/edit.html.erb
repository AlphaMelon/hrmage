<div class="row">
  <div class="large-9 large-centered small-12 columns">
    <fieldset>
      <legend><%= t :edit %> <%= @organization.name %></legend>
      <div class="panel"><%= t :organizations_edit_description %></div>
      <%= render 'form' %>
    </fieldset>
  </div>
</div>
