created a new position named 
<% if activity.trackable %>
  <%= link_to activity.trackable.name, organization_positions_path(current_organization) %>
<% else %>
  which has since been removed 
<% end %>
