<% if !@departments.nil? || !@department.nil? %>
  <%= render "shared/admin_panel" %>
<%end%>

<% if !@positions.nil? || !@position.nil? %>
  <%= render "shared/admin_panel" %>
<%end%>

<% if !@employees.nil? || !@employee.nil? %>
 <%= render "shared/admin_panel" %>
<%end%>

<% if !@leave_types.nil? || !@leave_type.nil? %>
 <%= render "shared/admin_panel" %>
<%end%>

<% if !@leaves.nil? || !@leave.nil? %>
  <%= render "shared/leaves_panel" %>
  <%= render "shared/admin_leaves_panel" %>
<%end%>

<% if !@my_leaves.nil? || !@my_leave.nil? %>
  <%= render "shared/leaves_panel" %>
  <%= render "shared/admin_leaves_panel" %>
<%end%>

<% if !@claims.nil? || !@claim.nil? %>
  <%= render "shared/claims_panel" %>
  <%= render "shared/admin_claims_panel" %>
<% end %>